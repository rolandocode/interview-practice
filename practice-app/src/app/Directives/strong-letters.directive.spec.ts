import { StrongLettersDirective } from './strong-letters.directive';

describe('StrongLettersDirective', () => {
  it('should create an instance', () => {
    const directive = new StrongLettersDirective();
    expect(directive).toBeTruthy();
  });
});
